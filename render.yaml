services:
  - type: web
    name: popkidallsites
    runtime: static
    buildCommand: bun install && bun run build
    staticPublishPath: ./.output/public
    routes:
      - type: rewrite
        source: /*
        destination: /index.html